import 'package:flutter/material.dart';

class CatogeryPage extends StatelessWidget {
  const CatogeryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Catogory'),
      ),
    );
  }
}
